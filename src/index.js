const { productosBaratos } = require('./exercise_1');
const { descifrar } = require('./exercise_2');
const { persona } = require('./exercise_3');
const { max } = require('./exercise_4');

module.exports = {
  productosBaratos,
  descifrar,
  persona,
  max,
};
